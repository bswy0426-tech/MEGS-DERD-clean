inherit_from: configs/SimuEvent/replica_office3.yaml

data:
  datadir: ${REPLICA_EVENT_ROOT}/office3
  output: output/depth_error/office3_clean
  exp_name: clean_derdnet

depth_error_exp:
  enable: false
